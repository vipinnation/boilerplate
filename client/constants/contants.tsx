const APP_DETAILS = {
  name: 'Relic',
  description: 'Comprehensive school management solution for multiple branches'
};

const HOST = 'http://localhost:5000';
const BASE_URL = `${HOST}/api/v1`;

const CONSTANTS = { APP_DETAILS, HOST, BASE_URL };

export default CONSTANTS;
